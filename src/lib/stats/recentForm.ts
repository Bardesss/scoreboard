import type { AggregatorGame, AggregatorMember, RecentFormRow } from './types'

// Stub — implementation lands in a later task.
export function computeRecentForm(
  _games: AggregatorGame[],
  _members: AggregatorMember[],
  _viewerId: string | undefined,
): RecentFormRow[] | null {
  return null
}
